# @package _global_
defaults:
  - base/network
  - base/off_policy
  - base/streaming
  - base/vlm
  - base/on_policy
  - _self_

agent_type: animal_ppo

# [animal_ppo, animal_world_critic]
network_class: animal_ppo

# [lstm, gru]
temporal_model_type: gru
