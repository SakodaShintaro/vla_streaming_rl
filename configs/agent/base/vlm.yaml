# @package _global_
use_lora: 0
vlm_model_id: Qwen/Qwen3.5-2B
num_state_queries: 219
state_out_dim: 4
text_action_mode: none
use_prompt: 1
prompt_history_turns: 16
max_new_tokens: 128
temperature: 0.5
max_prompt_tokens: 512
pad_token_id: 0
text_q_margin: 1.0
# Decision rate of the environment, in Hz. The frame window goes through the
# backbone's native video channel, and this is what the plaintext timestamps
# labelling each temporal patch are written from; AnimalAI makes one decision
# per 5 physics steps of 0.02 s.
history_fps: 10.0
