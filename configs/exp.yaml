program: scripts/train.py
method: grid
parameters:
  env:
    value: placeholder
  learning_mode:
    values: [streaming, off_policy]
  seed:
    values: [0, 1, 2]
command:
  - ${env}
  - uv
  - run
  - python
  - ${program}
  - ${args_no_hyphens}
